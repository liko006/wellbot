# Contributing to Wellbot

Wellbot의 코드 변경은 [코딩 컨벤션](./docs/development/coding-convention.md)을
기준으로 한다.

## 개발·검증 환경 분리

개발 환경(로컬)과 실행·검증·운영 환경(사내망)이 물리적으로 분리되어 있다.
일반적인 "로컬에서 실행하며 개발"하는 흐름과 다르므로 아래를 전제로 작업한다.

| 환경 | 역할 |
|---|---|
| 로컬 | 코드 편집·커밋 전용. uv와 앱 실행 환경 없음 |
| QA(사내망) | 실행·기능 검증·테스트. uv 사용 가능 |
| PRD(사내망) | 운영. main 기준 수동 배포(pull + 재시작) |

- 실행·검증은 QA에서 `uv run reflex run --env prod`로 한다. QA와 PRD는 모두
  prod 모드(단일 포트, 핫리로드 없음)로 운영하므로 코드 반영에는 재시작이
  필요하다. nginx 구성의 저장소 참조본은 `docs/nginx-reflex.conf`다.
- 테스트는 QA에서 `uv run pytest`로 실행한다(CI 도입 후 GitHub Actions 병행).
  DB·AWS가 필요한 통합 검증은 사내망에서만 가능하다.
- **의존성 변경(pyproject.toml)은 uv.lock 재생성이 필요하므로 uv가 있는 QA에서
  작업한다.** pyproject.toml, uv.lock과 관련 코드를 같은 PR에 함께 담아 lock
  불일치 상태로 병합되지 않게 한다. 이런 소규모+lock 변경은 QA에서 직접
  편집·커밋하는 편이 낫고, 일반 코드 작업은 로컬에서 한다.
- main 병합은 배포가 아니다. PRD 반영은 별도의 수동 배포 단계다.

## 공개 저장소 주의

이 저장소는 public이다. secret, 내부 호스트·URL, 계정 정보, 운영 데이터와
개인정보를 커밋하지 않는다. 작업 계획·노트는 git이 무시하는 `plan/`에 두고,
저장소에는 `docs/`의 참조 문서만 커밋한다.

## 브랜치·커밋·스테이징

- main에서 작업 브랜치를 만들고 PR로 main에 병합한다.
- 브랜치는 관심사 단위로 묶는다(과도하게 잘게 쪼개지 않되, 성격이 다른 변경을
  한 브랜치에 섞지 않는다). 접두사: `feat/`, `fix/`, `perf/`, `chore/`,
  `refactor/`, `docs/`.
- 스테이징은 파일을 명시한다(`git add <path> ...`). `git add .`은 의도하지 않은
  파일이 섞이기 쉬우므로 사용하지 않는다.
- 커밋 제목은 명령형 `type: summary` 형식을 따른다(컨벤션 문서 22장).

## 변경 전 확인

1. 변경할 기능의 Page → State → Service → Model/API 흐름을 확인한다.
2. 인증, 소유권, 부서 권한과 비용 정책에 미치는 영향을 먼저 확인한다.
3. 기존 동작을 바꾸는 경우 테스트 또는 검증 시나리오를 먼저 정한다.
4. 대형 State에 기능을 바로 추가하기보다 독립된 service/use case로 분리할 수
   있는지 검토한다.

## Pull Request 기본 조건

- 변경 범위와 변경 이유가 설명되어 있다.
- 새 코드와 수정한 코드가 Wellbot 코딩 컨벤션을 따른다.
- 보안, 비용, 동시성, DB/S3 정합성 영향을 확인했다.
- 성공 경로뿐 아니라 권한 오류, 외부 서비스 실패와 재시도를 검증했다.
- 설정, DB schema 또는 운영 절차가 바뀌면 관련 문서도 함께 변경했다.
- 호환성을 위해 예외 규칙을 적용했다면 코드와 PR에 이유를 남겼다.

현재 저장소에는 formatter, linter, type checker와 pre-commit 자동화가 아직
적용되지 않았다. 자동화 도입 전까지는 컨벤션 문서의 PR 체크리스트를 사용하고,
도구가 도입된 뒤에는 동일 기준을 CI에서 강제한다.
